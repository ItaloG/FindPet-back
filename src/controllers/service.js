const Intituion = require("../models/Institution");

module.exports = {

    async store(req, res) {
        const { institutionId } = req;

        try {
            
        } catch (error) {
            
        }
        
    }

}